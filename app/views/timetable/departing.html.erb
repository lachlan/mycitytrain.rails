<% @title = "Departing" %>
<% @html_class = 'list' %>
<ul class="list">
	<% @stations.each do |station| %>
		<li><%= link_to (h station.name), :action => 'arriving', :departing => station.code %></li>
	<% end %>
</ul>